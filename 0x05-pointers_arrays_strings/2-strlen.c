#include "main.h"

/**
  * strlen - returns the length of a string
  * @s: string is evaluated
  * Returns: the length of string
  */

int _strlen(char *s);
{
	int i;

	while ( i = o; str[i] != '\0')
	{
		i++;
	}
	return (i);
}
